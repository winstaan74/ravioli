#summary User Tagging / Annotating of registry resources.
#labels Phase-Requirements

= Resource Tagging =
Is User Tagging / Annotating / Flagging of registry resources (as seen now in voexplorer) useful?

Should these annotations be shared between users?
  * What scope - all / by group?
  * how to determine which group the annotation belongs too


= Tagging of other things = 
Could we use ravioli to enable users to tag other things? - particular db tables, or even rows in db tables?
  * links in with idea of named subsets in topcat